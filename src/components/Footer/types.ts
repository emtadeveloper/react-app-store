export interface IFooter {
    news: { title: string; description: string; send: string; email: string };
    description: string;
    number: string;
    email: string;
    copy: string;
    map: string;
}

export type Event = { target: { value: string } };
